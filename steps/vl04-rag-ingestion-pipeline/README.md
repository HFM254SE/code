# VL04 RAG Ingestion Pipeline

## Setup

1. `VENV` Initialisieren

```bash
python3 -m venv .venv
```

2. `VENV` Laden

MacOS / Linux:

```bash
source .venv/bin/activate
```

Windows:

```bash
.venv\Scripts\activate
```

3. Dependencies Installieren

```bash
pip install -r requirements.txt
```
